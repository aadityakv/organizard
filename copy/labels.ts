// User-facing copy for the labels screens. Keys stay stable; only the strings change.
export const copy = {
  emptyBody: 'Add a box first, then come back to print its label.',
  emptyTitle: 'No boxes to print',
  introBody: "Each label has the box's QR code — scan it to jump straight to the box.",
  printLabelsTitle: 'Print labels',
} as const;
