# Agent guide

The project guide for coding agents lives in [`CLAUDE.md`](./CLAUDE.md). It is
tool-agnostic (what the app is, how to build, test and ship, and the gotchas), so
Codex and other agents should read that file rather than a copy that would drift.
